export enum ButtonPlace {
    Center = 'center',
    TheEnd = 'theEnd',
}

export enum ButtonDisplay {
    Present = 'present',
    Absent = 'absent',
}

export enum LocationHeader {
    Center = 'center',
    Edges = 'edges',
}

